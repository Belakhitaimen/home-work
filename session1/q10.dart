/**Explain what happens if you try to use a variable before it is declared in Dart */


/**in dart  if you try to use a variable before declaring it you will get a 
 * compile time error because dart is a statically typed language that requires
 * knowing a variable's type before usimg it
 */